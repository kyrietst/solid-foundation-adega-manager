# Task: Corrigir Layout do DatePicker

- [x] Localizar o componente DatePicker (`src/shared/ui/primitives/calendar.tsx`)
- [x] Corrigir alinhamento dos dias da semana
  - Alterar estrutura para usar `table-fixed` nativo e remover overrides de `flex`
- [x] Corrigir estilo de seleção (remover "quadrado amarelo")
  - `rounded-full` em `day_range_middle`
- [x] Corrigir erros de tipagem
  - Remover overrides de ícones que estavam causando conflito
- [x] Downgrade para v8 (fix final)
  - Remover import quebrado de CSS (`react-day-picker/style.css`)
  - Usar estilização Tailwind já implementada no componente
- [x] Corrigir largura esticada
  - Alterar `table` de `w-full` para `w-auto`
- [x] Corrigir erro de Deploy na Vercel (React 19 vs RDP v8)
  - Criar arquivo `.npmrc` com `legacy-peer-deps=true` para permitir a instalação
